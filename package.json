{
  "name": "endless-user-grid",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "postinstall": "bower install && gulp",
    "start": "node app.js > log 2>&1 &"
  },
  "dependencies": {
    "debug": "~2.1.1",
    "express": "~4.12.2"
  },
  "devDependencies": {
    "gulp": "^3.8.11",
    "gulp-browserify": "^0.5.1"
  }
}
